export interface IRecipe {
    id: number; //six numbers...number?
    image: string; //image url
    usedIngredients: string; //big array w/ objects n' stuff
    title: string; //name with quotes
    likes: number;
}